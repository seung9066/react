import RunawayButton from '@components/RunawayButton'

function Main ( props ) {

    return (
        <>
            <RunawayButton toastRef={props.props.toastRef} />
        </>
    )
}

export default Main;