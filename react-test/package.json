{
  "name": "react-test",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix client\" \"npm run dev --prefix server\" ",
    "dev:client": "vite",  
    "dev:server": "node server.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
